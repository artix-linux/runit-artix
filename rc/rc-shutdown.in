#!/bin/bash

. @RCDIR@/rc.conf
. @RCDIR@/functions

# avoid staircase effect
stty onlcr

echo " "
printhl "Initiating shutdown..."
echo " "

[[ -x @RCDIR@/rc.local.shutdown ]] && @RCDIR@/rc.local.shutdown

for shutdown in @RCDIR@/shutdown.d/*.sh; do
	. $shutdown
done
