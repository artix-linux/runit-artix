#!/bin/bash

# sourcing our current rc.conf requires this to be a bash script
. @RCDIR@/rc.conf
. @RCDIR@/functions

case "$1" in
	start)
		stat_busy "Starting kill-all"
		kill_all
		add_daemon kill-all
		stat_done
		;;
	*)
		echo "usage: $0 {start}"
		exit 1
esac
