#!/bin/bash

# sourcing our current rc.conf requires this to be a bash script
. @RCDIR@/rc.conf
. @RCDIR@/functions

case "$1" in
	start)
		stat_busy "Starting hostname"
		set_hostname
		add_daemon hostname
		stat_done
		;;
	*)
		echo "usage: $0 {start}"
		exit 1
esac
